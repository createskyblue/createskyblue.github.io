---
title: '使用VNA进行天线阻抗匹配'
coverText: 'VNA单端口校准与L型天线匹配'
date: '2026-06-29'
author: 'createskyblue'
email: 'createskyblue@outlook.com'
tags: ['VNA', '天线匹配', '史密斯圆图', '射频', '嵌入式']
coverImage: './PixPin_2026-05-28_01-52-46.jpg'
---

# 使用VNA进行天线阻抗匹配

![VNA匹配后的490MHz实测结果](./PixPin_2026-05-28_01-52-46.jpg)

开始焊元件前，先确认VNA要测的是标准`50 Ω`接口、固定天线系统，还是裸射频芯片的负载。本文记录一次`470MHz`至`510MHz`天线匹配，重点频点为`490MHz`。边界选错后，史密斯图即使回到圆心，结果也可能只适用于某一根天线。

## 测量对象与50 Ω前提

第一步只做一件事：确认VNA参考面向右看见什么负载。

| 测量对象 | 匹配目标 | 操作边界 |
| :--- | :--- | :--- |
| 裸射频芯片的RF引脚 | 芯片手册规定的最佳负载 | 使用芯片厂商的参考网络，不能默认目标是`50 + j0 Ω` |
| 无线模块的50 Ω ANT端口 | 标准`50 Ω`系统 | 可以按本文流程检查板上通路和天线负载 |
| 板载天线或固定外接天线 | 最终装配状态下的整体阻抗 | 天线保持连接，PI网络匹配整个系统 |
| 可替换的50 Ω外接天线 | 通用`50 Ω`接口 | 假负载验证板上通路，实际天线用于最终验收 |

芯片拆除后，VNA只能测到参考面之后的无源负载网络，无法反推出芯片的输出阻抗。本文以下流程成立的前提是：被拆器件的天线端口原本按`50 Ω`系统设计。裸芯片RF引脚需要先查数据手册或官方参考设计。

测量前完成四项检查：

1. 拆除射频芯片或无线模块，或切断其RF引脚与测量节点的连接
2. 关闭整板电源，确认没有发射功率进入VNA端口
3. 焊接和返修时断开VNA
4. 校准后固定射频线、转接头和焊点位置

仅关闭发射或给芯片断电还不够。未拆除的RF引脚内部网络仍会加载测量结果。

![射频线接入点与三个匹配焊盘的位置](./PixPin_2026-05-28_01-09-42.jpg)

图中`馈线#4`接到拆除器件后的RF焊盘，`屏蔽层#5`接附近GND。射频线越短，接地回路越小，板级校准的重复性越好。

## 板级OSL校准与参考面

本文只测单端口`S11`，使用`OPEN`、`SHORT`、`LOAD`完成OSL校准。目标是把参考面移到`焊盘#1`所在的匹配网络入口。

1. 设置扫频范围为`470MHz`至`510MHz`，重点观察`490MHz`
2. 焊好`馈线#4`和`屏蔽层#5`，保持三个匹配焊盘为空
3. 连接VNA，在空焊盘状态执行`OPEN`
4. 在`焊盘#1`焊`0R`，执行`SHORT`，记录后取下电阻
5. 在`焊盘#1`焊`50R`，执行`LOAD`并保存校准配置

没有`50R`时，`51R`只能用于近似调试。普通贴片`0R`、`50R/51R`、焊盘和接地过孔都有寄生参数，不能等同于实验室级校准件。

校准完成后，取下`焊盘#1`的负载电阻，恢复开路。此时`焊盘#1`就是本次测量的近似参考面。

再次测量刚才的`LOAD`或`OPEN`，只能确认校准配置仍在生效，不能证明绝对精度。需要验证精度时，应使用独立的RF负载或经过表征的板级校准件。

修改扫频范围、移动射频线、更换转接头或返修焊点后，重新执行OSL校准。

## 外接天线的验证与整体匹配

校准完成后，从下面两条路径中选择一条。两条路径解决的问题不同，不要混用结论。

### 可替换天线的50 Ω接口验证

目标是让PCB提供稳定的通用接口，不把某一根天线的阻抗偏差固化到板上PI网络。

1. `焊盘#2`焊`0R`，`焊盘#1`和`焊盘#3`保持开路
2. IPEX端连接高质量`50 Ω`假负载
3. 从`焊盘#1`参考面测量`S11`和史密斯图
4. 偏差较大时检查微带线、IPEX、地过孔和焊接
5. 换上实际天线，在最终安装状态下完成验收

这里的`50 Ω`假负载只用于验证板上通路，不决定某一根天线的PI匹配值。

如果模块厂商要求固定的输出匹配元件，应先使用厂商推荐值。PCB微带线本身需要按叠层和线宽设计成`50 Ω`，不能依靠PI元件长期掩盖走线阻抗错误。

### 固定天线的整体匹配

最终产品固定使用同一根天线、馈线和外壳时，直接把它们作为实际负载参与匹配。

1. `焊盘#2`焊`0R`，两个并联焊盘保持开路
2. 接入最终天线、馈线和外壳
3. 在`焊盘#1`读取初始复阻抗
4. 调整PI网络，使目标频段满足要求
5. 更换天线、馈线或安装方式后重新测量

本文后面的`Z=10.50 + j44.00 Ω`实测案例属于固定天线整体匹配。

板载天线也走这条路径。它的辐射体、PCB地、净空区和外壳属于同一个天线系统，第一次测量就要保留实际天线结构。

![外接使用的偶极子FPC天线](./dipole-fpc-antenna.jpg)

图中样品标有`LoRa M433`。用于本文`490MHz`目标频点前，需要确认其规格覆盖`470MHz`至`510MHz`。如果它只针对`433MHz`设计，PI网络可以改善端口`S11`，但不能据此证明辐射效率合格。

偶极子对主板地平面的依赖通常低于单极子。没有集成巴伦或共模抑制时，同轴线外导体仍可能参与辐射，因此最终测量要固定线缆走向和安装姿态。

## PI网络与史密斯圆图调试

本文板上的三个焊盘对应以下电路关系：

| 焊盘 | 电路位置 | 初始状态 |
| :--- | :--- | :--- |
| `焊盘#1` | 模块侧并联到地 | 开路 |
| `焊盘#2` | 信号线上串联元件 | `0R` |
| `焊盘#3` | 天线侧并联到地 | 开路 |

VNA从`焊盘#1`参考面看进去，会看到并联支路、串联元件、走线、IPEX、馈线和天线。使用史密斯圆图从负载向信号源规划时，本文先处理`焊盘#2`的串联元件，再处理`焊盘#1`的并联元件。

### 初始阻抗与元件规划

1. 保持`焊盘#2`为`0R`并接上实际天线，记录`490MHz`阻抗`Z=10.50 + j44.00 Ω`
2. 打开[在线史密斯圆图工具](https://onlinesmithchart.com/)，将频率设置为`490MHz`
3. 把初始阻抗填入`DP0（BLACK BOX）`
4. 在`焊盘#2`规划`15pF`串联电容
5. 在`焊盘#1`规划`12pF`并联电容

![将VNA测得的初始阻抗填入BLACK BOX](./PixPin_2026-05-28_01-48-17.jpg)

在线工具中的`短路电容`表示并联到地的电容。下面的理想模型先加入串联电容，再加入并联电容，目标点会移动到史密斯图中心附近。

![匹配网络仿真：串联电容对应焊盘2，并联电容对应焊盘1](./PixPin_2026-05-28_01-49-16.jpg)

### 实测偏差与有效寄生参数

每次只更换一个元件并重新测量。本文加入串联电容后的实测点接近理想模型；加入两个元件后，VNA显示约`48 - j24 Ω`，偏差已经影响下一步选值。

1. 把`48 - j24 Ω`添加为阻抗标记`MK0`
2. 调整模型中的有效寄生参数，使曲线接近`MK0`
3. 在拟合后的模型中重新选择并联电容
4. 更换实际元件，复测目标频点和整个频段

![将两个匹配元件焊接后的实测阻抗添加为MK0标记](./PixPin_2026-05-28_01-50-03.jpg)

这里拟合的是焊盘、过孔、走线、焊接和器件封装形成的综合影响。工具里的`ESL`是有效模型参数，不能直接认定为电容本体的标称ESL。

![用有效寄生参数近似拟合实际测量点](./PixPin_2026-05-28_01-50-35.jpg)

拟合后，本文把并联电容从`12pF`调整为`10pF`，仿真目标重新回到圆心附近。

![调整并联电容数值后的仿真结果](./PixPin_2026-05-28_01-51-35.jpg)

## 单点结果与频段边界

最终实测结果如下：

| 项目 | 读数/参数 | 工程判断 |
| :--- | :--- | :--- |
| 测量参数 | `S11` | 表示参考面处整个负载网络与`50 Ω`系统的匹配程度 |
| 标记频率 | `490.000000000 MHz` | 本文重点调试的单点频率 |
| 对数幅度 | `-22.15 dB` | 反射功率约为`0.61%` |
| 复阻抗 | `57.44 - j3.89 Ω` | 接近`50 + j0 Ω` |
| 驻波比 | `1.169` | 单点匹配结果较好 |

反射功率由`|Γ|² = (10^(-22.15/20))²`计算，结果约为`0.61%`。该数据与VNA显示的复阻抗和SWR一致。

完成单点匹配后，再检查四项边界：

1. 查看`470MHz`至`510MHz`内最差的`S11`或最大的`SWR`
2. 频段两侧偏差持续较大时，优先检查天线本身的带宽
3. 在最终外壳、电池、线缆走向和安装姿态下复测
4. 使用辐射效率、增益或通信距离测试判断整机性能

匹配网络可以移动阻抗点，无法把窄带天线变成宽带天线。`490MHz`处的良好`S11`只能证明端口反射较小，不能单独证明天线效率或通信距离。

> 本文由 AI 辅助生成，可能存在错误或遗漏，请以实际资料和官方文档为准。
